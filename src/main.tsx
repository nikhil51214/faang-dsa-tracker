import React from 'react'
import ReactDOM from 'react-dom/client'
import FAAnGDSATracker from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FAAnGDSATracker />
  </React.StrictMode>,
)
